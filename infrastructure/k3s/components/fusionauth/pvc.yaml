apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fusionauth-data
  namespace: develop
  labels:
    app: fusionauth
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi