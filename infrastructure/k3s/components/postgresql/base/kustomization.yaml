apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - service.yaml
  - configmap-init.yaml
  - configmap-config.yaml
  - networkpolicy.yaml

images:
  - name: postgres
    newTag: "18"